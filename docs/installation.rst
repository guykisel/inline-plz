.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install inline-plz

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv inline-plz
    $ pip install inline-plz
